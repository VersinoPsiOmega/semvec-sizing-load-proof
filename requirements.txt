# ---------------------------------------------------------------------------
# Runtime dependencies for the three pillars.
#
#   pillar 1 (sizing/)  — needs only PyYAML; no GPU, no semvec, no network.
#   pillar 2 (harness/) — needs semvec + sentence-transformers + tiktoken.
#                         Runs air-gapped without an LLM; --llm/--judge add one.
#   pillar 3 (load/)    — needs semvec[api] + psutil, a SEMVEC_LICENSE_KEY and
#                         k6 on PATH (https://k6.io — static binary, stageable
#                         offline).
#
# semvec itself is commercial software under a proprietary licence; see
# NOTICE.md. This repository (the harnesses, models and results) is Apache-2.0.
#
# Pinned as lower bounds so the harnesses keep working against newer releases.
# Exact versions this repository was last verified against are noted per line;
# that verification run included major-version jumps over the lower bounds
# (sentence-transformers 5.x -> 6.x, transformers 4.x -> 5.x) with the full
# test suite green, so the bounds are deliberately permissive.
# ---------------------------------------------------------------------------

semvec[api,benchmarks,hybrid,jwt,mem0]>=0.8.8   # verified: 0.8.8
sentence-transformers>=5.6.0                    # verified: 6.0.1
torch>=2.12.0                                   # verified: 2.14.0 (CUDA build recommended)
transformers>=4.57.6                            # verified: 5.16.1
tiktoken>=0.13.0                                # verified: 0.14.0
PyYAML>=6.0.3                                   # verified: 6.0.3
psutil>=7.2.2                                   # verified: 7.2.2
numpy>=2.4.6                                    # verified: 2.5.3
